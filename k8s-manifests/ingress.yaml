apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cafe-ui-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: cafe-ui-service
            port:
              number: 80